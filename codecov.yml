coverage:
  status:
    project:
      default: off
      env:
        flags:
          - env
      redis:
        flags: 
          - redis
      test-utils:
        flags:
          - test-utils
      ws:
        flags:
          - ws

flags:
  env:
    paths:
      - packages/env/
  redis:
    paths:
      - packages/redis/
  test-utils:
    paths:
      - packages/test-utils/
  ws:
    paths:
      - packages/ws/
