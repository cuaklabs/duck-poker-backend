coverage:
  status:
    project:
      default: off
      env:
        flags:
          - env
      mongodb:
        flags:
          - mongodb
      redis:
        flags: 
          - redis
      testUtils:
        flags:
          - testUtils
      ws:
        flags:
          - ws

flags:
  env:
    paths:
      - packages/env/
  mongodb:
    paths:
      - packages/mongodb/
  redis:
    paths:
      - packages/redis/
  testUtils:
    paths:
      - packages/testUtils/
  ws:
    paths:
      - packages/ws/
