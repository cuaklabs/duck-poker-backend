coverage:
  status:
    project:
      default: off
      env:
        flags:
          - env
      redis:
        flags: 
          - redis
      testUtils:
        flags:
          - testUtils
      ws:
        flags:
          - ws

flags:
  env:
    paths:
      - packages/env/
  redis:
    paths:
      - packages/redis/
  testUtils:
    paths:
      - packages/testUtils/
  ws:
    paths:
      - packages/ws/
