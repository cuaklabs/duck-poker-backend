coverage:
  status:
    project:
      default: off
      env:
        flags:
          - env
      redis:
        flags: 
          - redis

flags:
  env:
    paths:
      - packages/env/
  redis:
    paths:
      - packages/redis/
